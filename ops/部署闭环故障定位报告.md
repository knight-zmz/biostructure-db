# 部署闭环故障定位报告

**报告时间**: 2026-03-30 00:10 CST  
**报告人**: AI Agent (单主控模式)  
**故障现象**: GitHub Actions 部署后 PM2 未自动重启

---

## 1. 故障现象描述

### 1.1 测试推送

**提交**: `9121d66 test: trigger GitHub Actions deploy verification`  
**推送时间**: 2026-03-29 23:35:55 CST  
**推送分支**: `main`  
**测试文件**: `.github_deploy_test.md`

### 1.2 预期行为链

```
推送 → GitHub Actions 触发 → Deploy via SSH → Restart PM2 → PM2 重启 → 健康检查
  │         │                   │               │            │           │
  │         │                   │               │            │           └─ ✅ 通过
  │         │                   │               │            └─ ❌ 未观察到
  │         │                   │               └─ 🔴 无法确认
  │         │                   └─ 🔴 无法确认
  │         └─ 🔴 无法确认
  └─ ✅ 已推送
```

### 1.3 实际观察结果

| 验证点 | 预期 | 实际观察 | 状态 |
|--------|------|----------|------|
| 推送到 GitHub | 成功 | `git push` 成功 | ✅ 已验证 |
| 文件存在于生产目录 | 存在 | `/var/www/myapp/.github_deploy_test.md` 存在 | ✅ 已验证 |
| workflow 触发 | 应触发 | 无法直接查看 | 🔴 无法直接确认 |
| Deploy via SSH | 应执行 | 无服务器端 SSH 日志 | 🔴 无法直接确认 |
| Restart PM2 | 应执行 | 无 PM2 自动重启日志 | 🔴 无法直接确认 |
| PM2 重启 | restarts+1 | restarts=1 (手动重启) | ❌ 未观察到 |
| 健康检查 | 通过 | `/api/health` 返回 healthy | ✅ 已验证 |

---

## 2. 证据边界声明

### 2.1 可直接验证的证据

| 证据 | 来源 | 状态 |
|------|------|------|
| Git 推送成功 | `git log`, `git push` 输出 | ✅ 已验证 |
| 文件存在于生产目录 | `ls -la /var/www/myapp/.github_deploy_test.md` | ✅ 已验证 |
| SSH 公钥已授权 | `grep biostructure ~/.ssh/authorized_keys` 有结果 | ✅ 已验证 |
| PM2 正常运行 | `pm2 list` 显示 online | ✅ 已验证 |
| PM2 无自动重启 | `pm2 describe myapp \| grep restarts` = 1 | ✅ 已验证 |
| 应用健康检查 | `curl /api/health` 返回 healthy | ✅ 已验证 |

### 2.2 无法直接确认的事项

| 事项 | 原因 | 验证责任 |
|------|------|----------|
| GitHub Secrets 配置状态 | 无法读取 GitHub Settings 页面 | 用户 |
| workflow 是否触发 | 无法访问 GitHub Actions 页面 | 用户 |
| Deploy via SSH 步骤日志 | 无法访问 GitHub Actions 页面 | 用户 |
| Restart PM2 步骤日志 | 无法访问 GitHub Actions 页面 | 用户 |
| SSH 连接是否尝试 | 无服务器端 SSH 连接日志 | 用户/日志 |

---

## 3. 候选原因列表

按证据强弱排序 (从强到弱):

### 候选原因 1: SSH 连接失败

**可能性**: 🟡 中

**现有证据**:
- ✅ SSH 公钥已添加到 `~/.ssh/authorized_keys`
- ❓ GitHub 端 `DEPLOY_SSH_KEY` 是否与公钥匹配 (无法确认)
- ❓ `DEPLOY_HOST` 是否正确 (无法确认)
- ❓ `DEPLOY_USER` 是否正确 (无法确认)

**支持证据**:
- 公钥已授权，但无法确认 GitHub 端私钥配置

**反对证据**:
- 无服务器端 SSH 连接失败日志 (如 `/var/log/secure`)

**验证方法**:
```bash
# 用户查看 GitHub Actions 日志
# 访问 https://github.com/knight-zmz/biostructure-db/actions
# 查看 "Deploy via SSH" 步骤日志
# 寻找 "Permission denied" 或 "Connection refused" 等错误
```

**证据等级**: 🟡 中 (公钥已授权，但私钥匹配性无法确认)

---

### 候选原因 2: Secrets 值错误

**可能性**: 🟡 中

**现有证据**:
- ❓ `DEPLOY_SSH_KEY` 值是否正确 (无法确认)
- ❓ `DEPLOY_HOST` 是否为服务器公网 IP (无法确认)
- ❓ `DEPLOY_USER` 是否为 `admin` (无法确认)

**支持证据**:
- 无法直接确认 Secrets 配置

**反对证据**:
- 无

**验证方法**:
```bash
# 用户检查 GitHub Secrets 配置
# 访问 https://github.com/knight-zmz/biostructure-db/settings/secrets/actions
# 确认:
# - DEPLOY_SSH_KEY = cat ~/.ssh/biostructure_db_deploy (私钥内容)
# - DEPLOY_HOST = 服务器公网 IP
# - DEPLOY_USER = admin
```

**证据等级**: 🟡 中 (完全无法直接确认)

---

### 候选原因 3: workflow 未触发

**可能性**: 🟢 低

**现有证据**:
- ✅ 推送到 `main` 分支 (应触发)
- ✅ `.github/workflows/deploy.yml` 存在
- ✅ workflow 配置 `on: push: branches: [ main ]`

**支持证据**:
- 无

**反对证据**:
- 推送成功，分支匹配，应触发

**验证方法**:
```bash
# 用户查看 GitHub Actions 页面
# 访问 https://github.com/knight-zmz/biostructure-db/actions
# 查看是否有新的 workflow run
```

**证据等级**: 🟢 低 (不太可能，配置正确)

---

### 候选原因 4: Deploy via SSH 步骤失败

**可能性**: 🟡 中

**现有证据**:
- ✅ 文件存在于生产目录 (可能是其他原因)
- ❓ Deploy 步骤是否执行 (无法确认)
- ❓ Deploy 步骤是否成功 (无法确认)

**支持证据**:
- 文件存在但 PM2 未重启，可能 Deploy 成功但 Restart 失败

**反对证据**:
- 文件存在可能是其他原因 (如手动同步)

**验证方法**:
```bash
# 用户查看 GitHub Actions 日志
# 查看 "Deploy via SSH" 步骤是否显示成功
# 寻找 rsync/ssh 命令执行日志
```

**证据等级**: 🟡 中 (文件存在但无法确认来源)

---

### 候选原因 5: Restart PM2 步骤失败

**可能性**: 🟢 低

**现有证据**:
- ✅ PM2 正常运行
- ✅ PM2 进程名 `myapp` 正确
- ❓ Restart 步骤是否执行 (无法确认)

**支持证据**:
- PM2 无自动重启记录

**反对证据**:
- PM2 正常运行，`pm2 restart myapp` 应成功

**验证方法**:
```bash
# 用户查看 GitHub Actions 日志
# 查看 "Restart PM2" 步骤日志
# 寻找 pm2 restart 命令执行结果
```

**证据等级**: 🟢 低 (PM2 正常，命令应成功)

---

## 4. 证据等级说明

| 等级 | 说明 |
|------|------|
| 🟢 低 | 有较强反面证据，可能性较低 |
| 🟡 中 | 证据不足，无法排除 |
| 🔴 高 | 有较强正面证据，可能性较高 |

---

## 5. 最小化验证动作

### 5.1 用户可执行的验证 (按优先级)

#### 验证 1: 查看 workflow 是否触发

```
访问: https://github.com/knight-zmz/biostructure-db/actions
查找: 最新 workflow run (应对应 commit 9121d66)
确认: workflow 是否显示为成功/失败/进行中
```

**预期**: 应有 1 个 workflow run

---

#### 验证 2: 查看 Deploy via SSH 步骤日志

```
在 workflow run 页面
点击: "Deploy via SSH" 步骤
查看: 日志输出
寻找: "Permission denied", "Connection refused", "Host key verification failed" 等错误
```

**预期**: 应显示 SSH 连接和文件传输日志

---

#### 验证 3: 查看 Restart PM2 步骤日志

```
在 workflow run 页面
点击: "Restart PM2" 步骤
查看: 日志输出
寻找: "pm2 restart myapp" 执行结果
```

**预期**: 应显示 PM2 重启成功

---

#### 验证 4: 检查服务器 SSH 日志

```bash
# 查看 SSH 连接日志
sudo grep -i "biostructure\|deploy" /var/log/secure | tail -20

# 或查看认证日志
sudo journalctl -u sshd | tail -50
```

**预期**: 如有 SSH 连接尝试，应有日志记录

---

### 5.2 AI 可执行的验证

#### 验证 1: 检查 PM2 日志

```bash
pm2 logs myapp --lines 50 --nostream | grep -E "2026-03-29T23:3[5-9]|restart"
```

**结果**: 无自动重启记录

---

#### 验证 2: 检查 SSH 授权

```bash
grep "biostructure" ~/.ssh/authorized_keys
```

**结果**: ✅ 公钥已授权

---

#### 验证 3: 检查 PM2 状态

```bash
pm2 describe myapp | grep -E "restarts|uptime"
```

**结果**: restarts=1 (手动重启)

---

## 6. 故障定位流程图

```
[开始]
  │
  ▼
workflow 是否触发？ ──(否)──→ [原因 3: workflow 未触发]
  │ (是)
  ▼
Deploy via SSH 是否成功？ ──(否)──→ [原因 1: SSH 连接失败]
  │ (是)                        或 [原因 2: Secrets 值错误]
  ▼
Restart PM2 是否执行？ ──(否)──→ [GitHub Actions 配置问题]
  │ (是)
  ▼
PM2 是否重启？ ──(否)──→ [原因 5: PM2 重启命令失败]
  │ (是)
  ▼
[部署闭环成功]
```

---

## 7. 当前结论

### 7.1 已验证事项

- ✅ 测试文件已推送到 GitHub
- ✅ 测试文件存在于生产目录
- ✅ SSH 公钥已添加到服务器
- ✅ PM2 正常运行
- ✅ 应用健康检查通过
- ❌ PM2 无自动重启记录

### 7.2 无法直接确认事项

- 🔴 GitHub Secrets 配置状态
- 🔴 workflow 是否触发
- 🔴 Deploy via SSH 步骤是否执行
- 🔴 Restart PM2 步骤是否执行

### 7.3 候选原因 (按可能性排序)

| 排名 | 候选原因 | 可能性 | 证据等级 |
|------|----------|--------|----------|
| 1 | SSH 连接失败 | 🟡 中 | 公钥已授权，私钥匹配性未知 |
| 2 | Secrets 值错误 | 🟡 中 | 完全无法直接确认 |
| 3 | Deploy 步骤失败 | 🟡 中 | 文件存在但来源未知 |
| 4 | workflow 未触发 | 🟢 低 | 配置正确，应触发 |
| 5 | PM2 重启命令失败 | 🟢 低 | PM2 正常，应成功 |

---

## 8. 下一步行动

### 8.1 立即行动 (用户)

1. 访问 https://github.com/knight-zmz/biostructure-db/actions
2. 查看最新 workflow run 状态
3. 查看 "Deploy via SSH" 步骤日志
4. 查看 "Restart PM2" 步骤日志
5. 记录任何错误信息

### 8.2 后续行动 (根据日志)

根据日志定位具体失败环节后:
- 如 SSH 连接失败 → 检查私钥/公钥匹配
- 如 Secrets 值错误 → 修正 Secret 值
- 如 workflow 未触发 → 检查分支配置
- 如 Deploy 失败 → 检查路径/权限
- 如 PM2 重启失败 → 检查进程名

---

## 9. 文档修正记录

### 9.1 修正前错误表述

| 错误表述 | 位置 | 修正后 |
|----------|------|--------|
| "GitHub Secrets 未配置" | project_state.md 0.3 | "Secrets 配置状态无法直接确认" |
| "Secrets 未配置" | project_state.md 风险清单 | "部署闭环验证失败，候选原因待定位" |
| "等待用户配置 Secrets" | backlog.md | "用户查看 GitHub Actions 日志" |

### 9.2 修正原则

1. 不将"无法直接验证"写成"已知原因"
2. 对 GitHub Settings/Actions 页面内容标记为"无法直接确认"
3. 失败原因写成"候选原因列表"而非单一断言
4. 按证据强弱排序候选原因

---

**报告状态**: ✅ 完成  
**提交状态**: ⏳ 待提交  
**证据完整性**: ✅ 已区分已验证/无法直接确认事项
